<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.aarya.retrofitday2.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:id="@+id/tv"
        android:layout_height="wrap_content"
        android:textSize="20dp"
        android:text="Select any"
        />

    <Spinner
        android:layout_width="wrap_content"
        android:id="@+id/spinner"
        android:text="@string/display"
        android:layout_below="@+id/tv"
        android:layout_margin="10dp"
        android:background="@drawable/gradient_spinner"
        android:layout_height="wrap_content">
    </Spinner>

    <Spinner
        android:layout_width="wrap_content"
        android:id="@+id/get_spinner"
        android:text="@string/display"
        android:layout_toRightOf="@id/spinner"
        android:layout_below="@+id/tv"
        android:layout_marginRight="20dp"
        android:layout_margin="10dp"
        android:background="@drawable/gradient_spinner"
        android:layout_height="wrap_content">
    </Spinner>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/submitButton"
        android:layout_centerInParent="true"
        android:text="@string/submitDisplay"/>

    <Button
        android:layout_below="@+id/submitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/get_submitButton"
        android:layout_centerInParent="true"
        android:text="@string/submitDisplay1"/>
</RelativeLayout>
